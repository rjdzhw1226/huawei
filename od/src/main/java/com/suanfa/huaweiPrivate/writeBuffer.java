package com.suanfa.huaweiPrivate;

import java.util.Locale;

public class writeBuffer {
    //实现一个字节缓冲区
    //1 写入主存 偏移量 长度 写入数字
    //2 读主存 读之前如果缓存内有影响到的地址需要先写入 偏移量 长度
    //3 同步主存
    String bufferMethod(int bufferCup, int[][] condition, String data){

        return "";
    }

    String Integer2Hex(int num){
        String str = Integer.toHexString(num).toUpperCase(Locale.ROOT);
        return "";
    }
}
